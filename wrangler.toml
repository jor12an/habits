# Optional: CLI deploy with `npx wrangler pages deploy .`
# Git-connected Cloudflare Pages should use dashboard settings below.
name = "habits"
compatibility_date = "2026-07-24"
pages_build_output_dir = "."
