{
  "name": "habits",
  "private": true,
  "version": "1.3.0",
  "description": "Habits — private mobile-first habit tracker PWA (static site).",
  "scripts": {
    "build": "node -e \"console.log('Static site in public/ — no compile step')\"",
    "start": "python -m http.server 8080 --directory public",
    "deploy": "npx --yes wrangler@4 deploy"
  }
}
