{
  "name": "habits",
  "private": true,
  "version": "1.3.1",
  "description": "Habits — private mobile-first habit tracker PWA (static site).",
  "scripts": {
    "build": "echo Static site ready in public/",
    "start": "python -m http.server 8080 --directory public",
    "deploy": "npx --yes wrangler@4 deploy --config wrangler.toml"
  }
}
